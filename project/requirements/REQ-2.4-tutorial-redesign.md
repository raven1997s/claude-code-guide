# REQ-2.4: VS Code 插件教程重构

**需求类型**: Phase 2 - 功能增强
**优先级**: Must Have
**设计方向**: 渐进式引导 + 深度学习 + 自适应主题
**创建时间**: 2026-01-01

---

## 用户故事

**As a** 开发者,
**I want** 一个互动性强、实用性高的 VS Code 插件教程,
**So that** 我能快速掌握 Claude Code VS Code 扩展的使用方法，并在实际工作中应用

---

## 当前问题分析

### 现有问题
1. **信息密度过高** - 左侧文字堆砌，用户容易疲劳
2. **缺乏引导性** - 没有"亮点指示"，用户不知道该看哪里
3. **互动性不足** - 只是聊天面板，缺乏真实操作感
4. **进度可视化弱** - 只有简单数字，缺乏成就感
5. **缺乏实际场景** - 内容抽象，没有真实工作流
6. **移动端体验差** - 响应式布局不优雅

### 用户反馈（假设）
- "文字太多，没耐心看完"
- "不知道具体要点哪里"
- "看完还是不知道怎么用"
- "没有实际场景，学了就忘"

---

## 设计决策

根据用户确认：
- ✅ **教程风格**: A) 渐进式引导（一步步教，强制完成）
- ✅ **场景选择**: 全部场景（修复 Bug、代码重构、理解陌生代码）
- ✅ **时间投入**: 深度版 (30-60 分钟总计)
- ✅ **视觉风格**: C) 自适应主题（跟随系统/用户设置）

---

## 核心功能设计

### 1. 渐进式引导系统

#### 设计目标
- 每次**只展示一个核心概念**
- 使用**高亮动画**指示操作位置
- 提供**"跳过"选项**给有经验的用户

#### 交互流程
```
┌─────────────────────────────────────────────────────────────┐
│  [半透明遮罩层 - 暗化其他区域]                              │
│                                                             │
│  ┌───────────────────────────────────────────────┐         │
│  │  [聚光灯效果 - 高亮目标元素]                   │         │
│  │                                                │         │
│  │        ⚡ [这个图标会脉冲动画]                │         │
│  │                                                │         │
│  └───────────────────────────────────────────────┘         │
│                                                             │
│  ┌───────────────────────────────────────────────┐         │
│  │  💡 点击 Spark (⚡) 图标打开 Claude Code      │         │
│  │                                               │         │
│  │  这是你在 VS Code 中访问 Claude Code 的入口   │         │
│  │                                               │         │
│  │  [💡 提示] [我知道了，继续] [跳过教程]        │         │
│  └───────────────────────────────────────────────┘         │
└─────────────────────────────────────────────────────────────┘
```

#### 技术实现

**组件结构**:
```vue
<template>
  <div class="tutorial-overlay" v-if="currentStep">
    <!-- 半透明遮罩 -->
    <div class="overlay-backdrop" @click="handleBackdropClick"></div>

    <!-- 聚光灯高亮 -->
    <div
      class="spotlight"
      :style="spotlightStyle"
      :class="{ 'pulse': shouldPulse }"
    ></div>

    <!-- 指导卡片 -->
    <div class="instruction-card" :style="cardPosition">
      <div class="card-header">
        <span class="step-badge">步骤 {{ currentStep.number }}/{{ totalSteps }}</span>
        <button class="skip-btn" @click="skipTutorial">跳过教程</button>
      </div>

      <h3 class="card-title">
        <span class="icon">{{ currentStep.icon }}</span>
        {{ currentStep.title }}
      </h3>

      <p class="card-description">{{ currentStep.description }}</p>

      <!-- 智能提示系统 -->
      <div v-if="currentStep.hints" class="hints-section">
        <button
          v-for="(hint, index) in currentStep.hints"
          :key="index"
          class="hint-btn"
          :class="{ active: showHintLevel >= index }"
          @click="showHintLevel = index"
        >
          💡 提示 {{ index + 1 }}
        </button>
        <div v-if="showHintLevel >= 0" class="hint-text">
          {{ currentStep.hints[showHintLevel] }}
        </div>
      </div>

      <!-- 操作按钮 -->
      <div class="card-actions">
        <button
          v-if="canSimulate"
          class="btn-simulate"
          @click="simulateAction"
        >
          🎬 演给我看
        </button>
        <button
          class="btn-primary"
          @click="completeStep"
          :disabled="!canComplete"
        >
          我知道了 ✓
        </button>
      </div>
    </div>
  </div>
</template>

<script setup>
import { ref, computed } from 'vue'

const currentStep = ref(null)
const showHintLevel = ref(-1)
const canComplete = ref(false)

// 聚光灯位置计算
const spotlightStyle = computed(() => {
  if (!currentStep.value?.target) return {}
  const rect = currentStep.value.target.getBoundingClientRect()
  return {
    left: rect.left - 8 + 'px',
    top: rect.top - 8 + 'px',
    width: rect.width + 16 + 'px',
    height: rect.height + 16 + 'px'
  }
})

// 卡片位置（智能避开高亮区域）
const cardPosition = computed(() => {
  // 计算最佳位置...
  return { bottom: '32px', left: '50%', transform: 'translateX(-50%)' }
})

// 验证任务完成
function validateTask() {
  // 检测用户是否真的执行了操作
  canComplete.value = true
}
</script>

<style scoped>
.tutorial-overlay {
  position: fixed;
  inset: 0;
  z-index: 9999;
  pointer-events: none;
}

.overlay-backdrop {
  position: absolute;
  inset: 0;
  background: rgba(0, 0, 0, 0.7);
  backdrop-filter: blur(4px);
  pointer-events: auto;
}

.spotlight {
  position: absolute;
  border-radius: 8px;
  box-shadow: 0 0 0 9999px rgba(0, 0, 0, 0.7),
              0 0 0 4px var(--color-primary-500),
              0 0 20px var(--color-primary-500);
  pointer-events: auto;
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.spotlight.pulse {
  animation: spotlightPulse 2s infinite;
}

@keyframes spotlightPulse {
  0%, 100% {
    box-shadow: 0 0 0 9999px rgba(0, 0, 0, 0.7),
                0 0 0 4px var(--color-primary-500),
                0 0 20px rgba(99, 102, 241, 0.5);
  }
  50% {
    box-shadow: 0 0 0 9999px rgba(0, 0, 0, 0.7),
                0 0 0 8px var(--color-primary-500),
                0 0 40px rgba(99, 102, 241, 0.8);
  }
}

.instruction-card {
  position: absolute;
  background: var(--color-bg-elevated);
  border-radius: var(--radius-lg);
  padding: var(--spacing-6);
  box-shadow: var(--shadow-xl);
  max-width: 480px;
  pointer-events: auto;
  animation: cardSlideUp 0.3s var(--ease-out);
}

@keyframes cardSlideUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
</style>
```

**验收标准**:
- [ ] AC1: 聚光灯效果能准确定位到目标元素
- [ ] AC2: 卡片位置智能避让，不遮挡高亮区域
- [ ] AC3: 脉冲动画流畅，吸引注意力但不刺眼
- [ ] AC4: 跳过功能正常，退出后记录进度
- [ ] AC5: 移动端卡片位置自适应

---

### 2. 任务完成验证系统

#### 设计目标
- 用户**必须执行操作**才能继续
- 提供**多级提示**（3级）
- 防止**盲目点击**

#### 验证逻辑

```javascript
// 任务验证器
const taskValidators = {
  // 点击任务
  clickTarget: {
    validate: (event) => {
      return event.target === currentStep.value.targetElement
    },
    hints: [
      '点击侧边栏的 ⚡ 图标',
      '图标在 VS Code 左侧活动栏，是一个闪电形状',
      '直接点击这里模拟操作'
    ]
  },

  // 输入任务
  inputText: {
    validate: (value) => {
      return value.trim().length > 0
    },
    hints: [
      '在输入框中输入消息',
      '输入 "你好" 或任何你想问的内容',
      '点击这里自动填充示例消息'
    ]
  },

  // 发送任务
  sendMessage: {
    validate: (message) => {
      return message && message.role === 'user'
    },
    hints: [
      '按 Enter 键或点击发送按钮',
      'Shift+Enter 可以换行',
      '点击这里自动发送'
    ]
  },

  // 应用修改任务
  applyDiff: {
    validate: (action) => {
      return action === 'apply'
    },
    hints: [
      '点击差异下方的"应用"按钮',
      '按钮是绿色的，有一个对勾图标',
      '点击这里自动应用'
    ]
  }
}
```

**验收标准**:
- [ ] AC1: 每个任务都有明确的验证逻辑
- [ ] AC2: 提示级别递进，从隐晦到明确
- [ ] AC3: 防止用户跳过关键步骤（除非使用"跳过教程"）
- [ ] AC4: 验证失败时给出友好反馈

---

### 3. 成就与进度系统

#### 设计目标
- **徽章系统** - 完成里程碑获得徽章
- **进度可视化** - 清晰的进度条和百分比
- **庆祝动画** - 完成任务时的奖励反馈

#### 徽章设计

| 徽章 | 图标 | 名称 | 获得条件 |
|------|------|------|----------|
| 🚀 | 初学者 | 完成第一个步骤 |
| ⚡ | 闪电侠 | 在 30 秒内完成 Module 1 |
| 🎓 | 学者 | 阅读所有详细说明 |
| 🧠 | 理解者 | 完成所有场景模拟 |
| 🔧 | 工匠 | 完成代码重构场景 |
| 🐛 | 虫子猎人 | 完成 Bug 修复场景 |
| 🏆 | 大师 | 完成所有教程 |
| ⭐ | 完美主义 | 不使用任何提示完成 |

#### 进度追踪

```javascript
// 学习进度数据结构
const progress = {
  // 当前模块
  currentModule: 1,
  currentStep: 3,

  // 完成状态
  completedModules: [],
  completedSteps: [1, 2],

  // 时间统计
  startTime: '2026-01-01T10:00:00Z',
  timeSpent: 180, // 秒
  fastestTime: null,

  // 交互统计
  hintsUsed: 2,
  skipsUsed: 0,
  simulateUsed: 1,

  // 成就
  badges: ['🚀'],
  currentStreak: 3,

  // 场景完成度
  scenarios: {
    bugFix: { completed: false, score: 0 },
    refactor: { completed: false, score: 0 },
    understandCode: { completed: false, score: 0 }
  },

  // 最后访问
  lastAccessed: '2026-01-01T10:05:00Z'
}
```

#### 庆祝动画

```vue
<template>
  <Transition name="celebrate">
    <div v-if="showCelebration" class="celebration-overlay">
      <!-- 徽章展示 -->
      <div class="badge-reveal">
        <div class="badge-icon">{{ badge.icon }}</div>
        <h2 class="badge-name">{{ badge.name }}</h2>
        <p class="badge-desc">{{ badge.desc }}</p>
      </div>

      <!-- 粒子效果 -->
      <canvas ref="particlesCanvas" class="particles"></canvas>

      <!-- 继续按钮 -->
      <button class="continue-btn" @click="dismiss">
        继续学习 →
      </button>
    </div>
  </Transition>
</template>

<style scoped>
.celebration-overlay {
  position: fixed;
  inset: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: rgba(0, 0, 0, 0.9);
  z-index: 10000;
}

.badge-icon {
  font-size: 120px;
  animation: badgePop 0.6s var(--ease-bounce);
}

@keyframes badgePop {
  0% { transform: scale(0) rotate(-180deg); opacity: 0; }
  50% { transform: scale(1.2) rotate(10deg); }
  100% { transform: scale(1) rotate(0deg); opacity: 1; }
}

.celebrate-enter-active {
  animation: fadeIn 0.3s var(--ease-out);
}
</style>
```

**验收标准**:
- [ ] AC1: 完成任务立即触发庆祝动画
- [ ] AC2: 徽章正确记录到 localStorage
- [ ] AC3: 进度条实时更新百分比
- [ ] AC4: 可以分享成就到社交媒体

---

### 4. 真实场景模拟

#### 场景 1: 快速修复 Bug (10 分钟)

**场景设定**:
```
你正在开发一个功能，突然发现组件报错：
"TypeError: Cannot read property 'map' of undefined"

任务：使用 Claude Code 快速定位并修复这个 Bug
```

**步骤流程**:
```
1. [任务] 打开 Claude Code 面板
2. [任务] @-mention 引用报错的组件文件
3. [任务] 描述 Bug 现象
4. [任务] 查看 Claude 分析结果
5. [任务] 应用修复代码
6. [任务] 验证修复效果
```

**实现**:
```javascript
const bugFixScenario = {
  id: 'bug-fix',
  title: '快速修复 Bug',
  description: '使用 Claude Code 快速定位并修复组件报错',
  estimatedTime: '10 min',
  difficulty: '初级',

  // 预设代码（带 Bug）
  initialCode: `
<template>
  <div>
    <div v-for="item in items" :key="item.id">
      {{ item.name }}
    </div>
  </div>
</template>

<script setup>
import { ref } from 'vue'

const items = ref(null) // Bug: 应该是 []
</script>
  `,

  // 正确代码
  fixedCode: `
<template>
  <div>
    <div v-for="item in items" :key="item.id">
      {{ item.name }}
    </div>
  </div>
</template>

<script setup>
import { ref } from 'vue'

const items = ref([]) // 修复：初始化为空数组
</script>
  `,

  // 预期用户输入
  expectedInput: '@Component.vue 这个组件报错了，显示 "Cannot read property map of undefined"',

  // Claude 回复（模拟）
  claudeResponse: `
我发现了问题！

**Bug 原因**：
\`items\` 被初始化为 \`null\`，当模板尝试调用 \`items.map()\` 时就会报错。

**修复方案**：
将 \`const items = ref(null)\` 改为 \`const items = ref([])\`

这样 \`items\` 就是一个响应式数组，即使为空也可以调用 \`map()\` 方法。

让我帮你应用这个修复：
  `,

  // 验证逻辑
  validate: (userCode) => {
    return userCode.includes('ref([])')
  }
}
```

---

#### 场景 2: 代码重构 (15 分钟)

**场景设定**:
```
你接手了一个遗留组件，代码结构混乱，难以维护。
任务：使用 Claude Code 的计划模式重构这个组件
```

**步骤流程**:
```
1. [任务] @-mention 引用需要重构的组件
2. [任务] 发送重构请求
3. [任务] 查看计划模式生成的步骤
4. [任务] 编辑计划（添加 TypeScript 类型）
5. [任务] 确认并应用重构
6. [任务] 运行测试验证
```

---

#### 场景 3: 理解陌生代码 (15 分钟)

**场景设定**:
```
你需要修改一个不熟悉的文件，但不理解代码逻辑。
任务：使用 Claude Code 逐步理解代码
```

**步骤流程**:
```
1. [任务] @-mention 引用陌生文件
2. [任务] 询问代码功能
3. [任务] 询问特定函数的作用
4. [任务] 询问数据流向
5. [任务] 请求绘制流程图
```

---

### 5. 教程模块结构

#### Module 1: 快速上手 (5 分钟)

```
┌─────────────────────────────────────────┐
│  Module 1: 快速上手                     │
│  ⏱️ 预计时间: 5 分钟                    │
│  📚 难度: 初级                          │
└─────────────────────────────────────────┘

步骤 1.1: 安装扩展 (30s)
  任务: 在 VS Code 扩展市场搜索并安装
  验证: 检测扩展是否已安装

步骤 1.2: 认识界面 (1min)
  任务: 点击 Spark 图标，查看面板布局
  验证: 正确点击图标

步骤 1.3: 发送第一条消息 (1min)
  任务: 输入"你好并发送"
  验证: 消息成功发送

步骤 1.4: 查看回复 (30s)
  任务: 阅读 Claude 的回复
  验证: 阅读时间超过 3 秒

步骤 1.5: 快速测验 (2min)
  任务: 回答 3 个选择题
  验证: 全部答对
```

**验收标准**:
- [ ] AC1: 所有步骤必须在模拟环境中完成
- [ ] AC2: 完成后获得"初学者"徽章
- [ ] AC3: 可跳过，但跳过不计入成就

---

#### Module 2: 核心功能 (20 分钟)

```
┌─────────────────────────────────────────┐
│  Module 2: 核心功能                     │
│  ⏱️ 预计时间: 20 分钟                   │
│  📚 难度: 中级                          │
└─────────────────────────────────────────┘

步骤 2.1: @-mention 文件引用
  任务: 引用文件并让 Claude 解释代码

步骤 2.2: 计划模式
  任务: 使用计划模式重构代码

步骤 2.3: 查看代码差异
  任务: 理解 diff 视图的红色和绿色含义

步骤 2.4: 应用/拒绝修改
  任务: 选择性应用代码修改

步骤 2.5: 多对话管理
  任务: 创建新对话并切换

步骤 2.6: 场景模拟: 快速修复 Bug
  任务: 完整的 Bug 修复流程
```

---

#### Module 3: 高级技巧 (25 分钟)

```
┌─────────────────────────────────────────┐
│  Module 3: 高级技巧                     │
│  ⏱️ 预计时间: 25 分钟                   │
│  📚 难度: 高级                          │
└─────────────────────────────────────────┘

步骤 3.1: 斜杠命令
  任务: 使用 /help, /commit 等命令

步骤 3.2: 键盘快捷键
  任务: 使用 Ctrl+L, Ctrl+D 等快捷键

步骤 3.3: MCP 服务器
  任务: 配置并使用 MCP 服务器

步骤 3.4: 场景模拟: 代码重构
  任务: 使用计划模式完整重构流程

步骤 3.5: 场景模拟: 理解陌生代码
  任务: 逐步理解复杂代码逻辑

步骤 3.6: 综合实战
  任务: 独立完成一个完整工作流
```

---

### 6. 自适应主题

#### 主题检测

```javascript
// 检测用户主题偏好
const themeDetection = {
  // VS Code 主题
  vscodeTheme: computed(() => {
    // 通过 VS Code API 获取
    return vscode.env.appHostTheme || 'dark'
  }),

  // 系统主题
  systemTheme: computed(() => {
    if (window.matchMedia('(prefers-color-scheme: dark)').matches) {
      return 'dark'
    }
    return 'light'
  }),

  // 用户设置的主题
  userTheme: computed(() => {
    return localStorage.getItem('tutorial-theme') || 'auto'
  }),

  // 最终应用的主题
  appliedTheme: computed(() => {
    const user = themeDetection.userTheme.value
    if (user !== 'auto') return user
    return themeDetection.vscodeTheme.value
  })
}
```

**主题样式**:
```css
/* 浅色主题 */
[data-theme="light"] {
  --vscode-bg: #ffffff;
  --vscode-sidebar: #f3f3f3;
  --vscode-active: #e8e8e8;
  --vscode-border: #e0e0e0;
  --vscode-text: #333333;
}

/* 深色主题 */
[data-theme="dark"] {
  --vscode-bg: #1e1e1e;
  --vscode-sidebar: #252526;
  --vscode-active: #37373d;
  --vscode-border: #454545;
  --vscode-text: #cccccc;
}

/* 高对比度主题 */
[data-theme="high-contrast"] {
  --vscode-bg: #000000;
  --vscode-sidebar: #000000;
  --vscode-active: #333333;
  --vscode-border: #ffffff;
  --vscode-text: #ffffff;
}
```

**验收标准**:
- [ ] AC1: 自动检测 VS Code 当前主题
- [ ] AC2: 支持手动切换主题
- [ ] AC3: 主题切换平滑过渡
- [ ] AC4: 高对比度主题可访问性符合 WCAG AAA

---

## 交互流程图

```
开始
  │
  ├─→ 显示欢迎页面
  │   ├─→ 选择: 开始教程 | 跳过 | 查看大纲
  │
  ├─→ Module 1: 快速上手
  │   ├─→ 步骤 1.1: 安装扩展
  │   │   ├─→ [聚光灯] VS Code 扩展图标
  │   │   ├─→ 用户操作: 点击安装
  │   │   └─→ 验证: 扩展已安装 ✓
  │   │
  │   ├─→ 步骤 1.2: 认识界面
  │   │   ├─→ [聚光灯] Spark (⚡) 图标
  │   │   ├─→ [提示 1] 图标在左侧活动栏
  │   │   ├─→ [提示 2] 黄色闪电图标
  │   │   ├─→ [提示 3] 点击模拟
  │   │   └─→ 验证: 面板已打开 ✓
  │   │
  │   └─→ ... (更多步骤)
  │   └─→ Module 完成 → 获得徽章 🚀
  │
  ├─→ Module 2: 核心功能
  │   ├─→ 步骤 2.1: @-mention 文件引用
  │   ├─→ 步骤 2.2: 计划模式
  │   ├─→ 场景模拟: 快速修复 Bug
  │   └─→ Module 完成 → 获得徽章 🐛
  │
  ├─→ Module 3: 高级技巧
  │   ├─→ 步骤 3.1: 斜杠命令
  │   ├─→ 场景模拟: 代码重构
  │   ├─→ 场景模拟: 理解陌生代码
  │   └─→ Module 完成 → 获得徽章 🔧
  │
  └─→ 全部完成 → 获得徽章 🏆
      ├─→ 显示成就总结
      ├─→ 生成分享卡片
      └─→ 提供进阶学习资源
```

---

## 页面布局设计

### 桌面端 (>1024px)

```
┌──────────────────────────────────────────────────────────────────┐
│  [返回] VS Code 插件教程              进度: ████░░░░░ 40% [暂停] │
├──────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────┐  ┌───────────────────────────────────────────────┐  │
│  │          │  │                                               │  │
│  │  步骤    │  │                                               │  │
│  │  导航    │  │         VS Code 模拟界面                       │  │
│  │          │  │                                               │  │
│  │  ✓ M1-1  │  │   ┌────────────┐  ┌──────────────────────┐   │  │
│  │  ✓ M1-2  │  │   │            │  │  Claude Code 面板    │   │  │
│  │  → M1-3  │  │   │  编辑器    │  │                       │   │  │
│  │  ○ M1-4  │  │   │            │  │  [消息区域]          │   │  │
│  │  ○ M1-5  │  │   │            │  │                       │   │  │
│  │          │  │   └────────────┘  └──────────────────────┘   │  │
│  │  ───────  │  │                                               │  │
│  │  ✓ M2    │  │   [聚光灯高亮 + 指导卡片浮动]                 │  │
│  │  → M3    │  │                                               │  │
│  │          │  │                                               │  │
│  │  [徽章]  │  │                                               │  │
│  │  🚀⚡🎓  │  │                                               │  │
│  └──────────┘  └───────────────────────────────────────────────┘  │
│                                                                     │
└───────────────────────────────────────────────────────────────────┘
```

---

### 平板端 (768px - 1024px)

```
┌─────────────────────────────────────────────┐
│  [返回] VS Code 教程    进度 40% [菜单]     │
├─────────────────────────────────────────────┤
│                                               │
│  ┌─────────────────────────────────────────┐ │
│  │                                         │ │
│  │      VS Code 模拟界面                   │ │
│  │                                         │ │
│  │     [全屏显示，自动滚动到目标元素]       │ │
│  │                                         │ │
│  └─────────────────────────────────────────┘ │
│                                               │
│  ┌─────────────────────────────────────────┐ │
│  │  💡 当前步骤说明                         │ │
│  │  [可折叠展开更多详情]                   │ │
│  └─────────────────────────────────────────┘ │
└───────────────────────────────────────────────┘
```

---

### 移动端 (<768px)

```
┌───────────────────────────┐
│  [←] VS Code 教程    [...] │
├───────────────────────────┤
│                            │
│  ┌─────────────────────┐  │
│  │                     │  │
│  │  VS Code 模拟       │  │
│  │  (全屏显示)          │  │
│  │                     │  │
│  └─────────────────────┘  │
│                            │
│  [💡 浮动提示按钮]        │
│  点击显示步骤说明         │
└────────────────────────────┘
```

---

## 技术实现要点

### 核心技术栈

- **Vue 3 Composition API** - 响应式状态管理
- **Naive UI** - UI 组件库
- **Canvas API** - 粒子效果
- **Web Animations API** - 流畅动画
- **LocalStorage** - 进度持久化

### 关键功能实现

#### 1. 聚光灯定位算法

```javascript
// 智能定位高亮区域
function calculateSpotlight(targetElement) {
  const rect = targetElement.getBoundingClientRect()
  const viewport = {
    width: window.innerWidth,
    height: window.innerHeight
  }

  // 计算高亮区域（添加 padding）
  const spotlight = {
    x: rect.left - 12,
    y: rect.top - 12,
    width: rect.width + 24,
    height: rect.height + 24
  }

  // 边界检查
  spotlight.x = Math.max(0, spotlight.x)
  spotlight.y = Math.max(0, spotlight.y)

  return spotlight
}
```

#### 2. 卡片位置计算

```javascript
// 智能避开高亮区域
function calculateCardPosition(spotlight, viewport) {
  const cardWidth = 400
  const cardHeight = 200
  const padding = 20

  // 尝试放在高亮区域下方
  let position = {
    x: viewport.width / 2 - cardWidth / 2,
    y: spotlight.y + spotlight.height + padding
  }

  // 如果下方空间不足，放上方
  if (position.y + cardHeight > viewport.height - padding) {
    position.y = spotlight.y - cardHeight - padding
  }

  // 水平居中
  position.x = Math.max(
    padding,
    Math.min(
      viewport.width - cardWidth - padding,
      viewport.width / 2 - cardWidth / 2
    )
  )

  return position
}
```

#### 3. 进度持久化

```javascript
// 保存进度到 localStorage
function saveProgress(progress) {
  const data = {
    ...progress,
    lastSaved: new Date().toISOString()
  }
  localStorage.setItem('vscode-tutorial-progress', JSON.stringify(data))
}

// 加载进度
function loadProgress() {
  const saved = localStorage.getItem('vscode-tutorial-progress')
  if (saved) {
    try {
      return JSON.parse(saved)
    } catch (e) {
      return null
    }
  }
  return null
}

// 重置进度
function resetProgress() {
  localStorage.removeItem('vscode-tutorial-progress')
  localStorage.removeItem('vscode-tutorial-badges')
}
```

---

## 验收标准总览

### 功能验收

- [ ] **渐进式引导**
  - [ ] 聚光灯效果准确定位
  - [ ] 卡片位置智能避让
  - [ ] 多级提示系统
  - [ ] 跳过功能正常

- [ ] **任务验证**
  - [ ] 点击验证
  - [ ] 输入验证
  - [ ] 场景完成验证
  - [ ] 防止盲目点击

- [ ] **成就系统**
  - [ ] 徽章正确获得
  - [ ] 进度准确追踪
  - [ ] 庆祝动画流畅
  - [ ] 数据持久化

- [ ] **场景模拟**
  - [ ] Bug 修复场景
  - [ ] 代码重构场景
  - [ ] 理解代码场景
  - [ ] 每个场景可独立完成

- [ ] **响应式布局**
  - [ ] 桌面端完美显示
  - [ ] 平板端布局适配
  - [ ] 移动端功能完整
  - [ ] 横竖屏切换正常

### 性能验收

- [ ] 首屏加载 < 1s
- [ ] 动画帧率 60fps
- [ ] 交互延迟 < 100ms
- [ ] 内存占用 < 50MB

### 可访问性验收

- [ ] WCAG 2.1 AA 标准
- [ ] 键盘导航完整
- [ ] 屏幕阅读器友好
- [ ] 高对比度主题

---

## 工作量预估

| 模块 | 预估工时 | 技术难度 |
|------|----------|----------|
| **核心引擎** | | |
| 渐进式引导系统 | 2 MD | 高 |
| 任务验证系统 | 1.5 MD | 中 |
| 聚光灯定位算法 | 1 MD | 中 |
| **内容制作** | | |
| Module 1 内容 | 1 MD | 低 |
| Module 2 内容 | 2 MD | 中 |
| Module 3 内容 | 2 MD | 中 |
| 场景模拟开发 | 2 MD | 高 |
| **视觉效果** | | |
| 徽章与动画 | 1 MD | 中 |
| 响应式布局 | 1 MD | 中 |
| 主题系统 | 0.5 MD | 低 |
| **测试优化** | | |
| 功能测试 | 1 MD | - |
| 性能优化 | 0.5 MD | 中 |

**总预估**: 15.5 MD (约 4-5 周)

---

## 依赖项

- **前置需求**: Phase 1 全局体验优化完成
- **需要协调**: 产品经理（场景定义）、前端开发（技术实现）
- **需要测试**: 功能测试（完整场景测试）

---

## 成功指标

### 定量指标
- 教程完成率 > 70%
- 平均完成时间 < 45 分钟
- 用户满意度 > 4.5/5
- 跳过率 < 20%

### 定性指标
- 用户反馈"终于学会用了"
- 用户反馈"场景很实用"
- 用户反馈"比文档好懂多了"

---

**需求创建**: 2026-01-01
**产品经理**: 资深技术产品经理
**状态**: 🟢 已定义 - 待确认优先级