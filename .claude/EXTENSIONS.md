# Claude Code 扩展快速参考

> 本项目已配置智能扩展系统，AI 会根据你的需求自动选择合适的工具。

---

## 🎯 你不需要记住任何命令！

AI 会自动识别你的意图并调用正确的扩展。只需要自然地描述你的需求即可：

| 你说 | AI 自动做 |
|------|----------|
| "帮我写测试" | 调用 test-writer 生成测试 |
| "解释这段代码" | 调用 code-explainer |
| "有个 bug 修不好" | 切换到 debugger agent |
| "审查一下代码" | 调用 code-reviewer |
| "这个 Vue 组件怎么写" | 调用 vue-expert |

---

## 📋 已安装扩展清单

### Skills (技能)

| Skill | 功能 | 调用方式 |
|-------|------|----------|
| test-writer | 自动生成测试代码 | 说"写测试" |
| code-explainer | 解释代码工作原理 | 说"解释代码" |
| code-reviewer | 审查代码质量 | 说"审查代码" |
| vue-expert | Vue 3 最佳实践 | 说"Vue 专家" |
| frontend-design | 前端设计建议 | 说"设计建议" |

### Agents (智能体)

| Agent | 功能 | 何时自动切换 |
|-------|------|--------------|
| debugger | 调试复杂问题 | 提到"bug"、"错误" |
| test-runner | 运行并分析测试 | 提到"运行测试" |
| vue-expert | Vue 开发专家 | 提到"Vue 组件开发" |
| reviewer | 代码审查专家 | 提到"深度审查" |

### Hooks (自动触发)

| Hook | 触发时机 | 作用 |
|------|----------|------|
| pre-commit | Git 提交前 | 运行 ESLint + 测试 |
| post-write | 写入文件后 | 自动格式化代码 |
| pre-bash | 危险命令前 | 确认提示 (rm -rf 等) |

---

## 💡 使用示例

### 自动调用场景

```
你: "帮我为 SearchBox.vue 写测试"
→ AI: 自动调用 test-writer
→ 生成: SearchBox.test.js

你: "这个 search-data.js 不太懂"
→ AI: 自动调用 code-explainer
→ 输出: 详细代码解释

你: "搜索功能有 bug，结果不对"
→ AI: 自动切换到 debugger
→ 开始: 系统性调试流程

你: "帮我审查这个组件"
→ AI: 自动调用 code-reviewer
→ 输出: 代码质量报告
```

---

## 🔄 手动调用 (可选)

如果需要手动调用：

```bash
# 调用 Skill
/skill test-writer
/skill code-explainer

# 切换 Agent
/agent debugger
/agent vue-expert
```

---

## 📝 配置文件

- 配置文件: [`.claude/settings.json`](../.claude/settings.json)
- Hooks 目录: [`.claude/hooks/`](../.claude/hooks/)
- Skills 目录: [`.claude/skills/`](../.claude/skills/)
- Agents 目录: [`.claude/agents/`](../.claude/agents/)

---

## 🆘 获取帮助

随时问 AI：
- "有哪些可用的扩展？"
- "怎么使用 test-writer？"
- "当前启用了哪些 hooks？"
