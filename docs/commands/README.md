# 内置斜杠命令

在 Claude Code 交互式会话中，可以使用以下斜杠命令。

## 命令列表

| 命令 | 功能 |
|------|------|
| `/help` | 显示帮助信息和可用命令 |
| `/clear` | 清除当前对话上下文 |
| `/commit` | 创建 Git 提交 |
| `/tasks` | 查看和管理任务列表 |
| `/exit` | 退出会话 |
| `/editor` | 打开编辑器 |
| `/continue` | 继续上次响应 |
| `/skip` | 跳过当前操作 |
| `/accept` | 接受待处理操作 |
| `/reject` | 拒绝待处理操作 |

## /help

显示帮助信息。

```bash
> /help

# Claude Code 命令参考
#
# /help      - 显示此帮助
# /clear     - 清除对话历史
# /commit    - 创建 Git 提交
# /tasks     - 查看任务列表
# /exit      - 退出
```

## /clear

清除当前对话的上下文和记忆。

```bash
> /clear

# 对话上下文已清除
```

**使用场景：**
- 开始新话题
- 重置对话状态
- 清除错误上下文

## /commit

创建 Git 提交，Claude 会自动分析改动并生成提交信息。

```bash
> /commit

# Claude 会：
# 1. 运行 git status 查看改动
# 2. 运行 git diff 查看具体变更
# 3. 分析改动内容
# 4. 生成符合规范的提交信息
# 5. 执行 git add 和 git commit
```

**工作流程：**

1. 分析所有改动文件
2. 读取相关代码上下文
3. 生成清晰的提交信息
4. 按模块或功能分组提交
5. 添加 Co-Authored-By 标记

**示例输出：**

```
feat: add user authentication system

- Implement login/registration endpoints
- Add JWT token validation
- Create user middleware for protected routes

🤖 Generated with Claude Code

Co-Authored-By: Claude <noreply@anthropic.com>
```

## /tasks

查看和管理当前会话的任务列表。

```bash
> /tasks

# 当前任务：
#
# [ ] 1. 分析现有代码结构
# [ ] 2. 设计新的 API 接口
# [ ] 3. 实现用户认证
# [ ] 4. 编写单元测试
```

**功能：**
- 查看待办事项
- 跟踪进度
- 优先级管理

## /exit

退出 Claude Code 会话。

```bash
> /exit

# 或者使用快捷键：
# Ctrl+D (Unix/Linux/macOS)
# Ctrl+Z 然后 Enter (Windows)
```

**会话保存：**
- 默认自动保存会话
- 可通过 `--no-session-persistence` 禁用
- 使用 `claude -c` 继续上次会话

## /editor

打开默认编辑器编辑长消息。

```bash
> /editor

# 打开 $EDITOR 或 vim 编辑器
# 编辑完成后保存退出
```

**使用场景：**
- 编写长提示词
- 多行代码输入
- 复杂问题描述

## /continue

继续生成上次被截断的响应。

```bash
> /continue

# 继续上次的输出...
```

**使用场景：**
- 响应被 `max_tokens` 截断
- 需要更多细节

## /skip

跳过当前正在执行的工具调用。

```bash
> /skip

# 跳过当前操作，继续下一个
```

**使用场景：**
- 工具执行时间过长
- 不需要当前操作结果

## /accept

接受所有待处理的权限请求。

```bash
> /accept

# 批准所有挂起的操作
```

**等价于：** 全选权限提示中的 "Always allow"

## /reject

拒绝所有待处理的权限请求。

```bash
> /reject

# 拒绝所有挂起的操作
```

**等价于：** 全选权限提示中的 "Always deny"

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+C` | 中断当前生成 |
| `Ctrl+D` | 退出会话 |
| `Ctrl+L` | 清屏 |
| `↑ / ↓` | 浏览历史命令 |
| `Tab` | 自动补全 |

## 高级用法

### 组合使用

```bash
# 先清除上下文，然后开始新任务
> /clear
> 帮我实现一个新的功能

# 查看任务后继续工作
> /tasks
> /continue

# 完成后提交
> /commit
```

### 在提示中使用命令

```bash
# 在对话中引用命令
> 帮我分析这个文件，然后根据结果 /commit
```

## 配置

### 禁用斜杠命令

```bash
# 启动时禁用
claude --disable-slash-commands
```

### 自定义命令

在 `.claude/commands/` 创建自定义命令：

```
.claude/
└── commands/
    ├── deploy.sh
    └── test.sh
```

然后在会话中：
```bash
> /deploy
# 执行 .claude/commands/deploy.sh
```

## 注意事项

1. **命令前缀**：所有命令必须以 `/` 开头
2. **大小写敏感**：命令名小写
3. **上下文保留**：`/clear` 后会话仍在，只是清除对话历史
4. **权限影响**：`/accept` 和 `/reject` 影响待处理的权限请求
5. **退出保存**：`/exit` 前确保重要内容已保存
